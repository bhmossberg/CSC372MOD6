package module6;

import java.util.Comparator;
import java.util.List;

public class SelectionSort {

    /**
     * Sorts the given list using Selection Sort and the provided comparator.
     * @param list the list to sort
     * @param comparator the comparator to determine order
     */
    public static void sort(List<Student> list, Comparator<Student> comparator) {
        int n = list.size();

        // TODO: Implement Selection Sort algorithm here
        // One outer loop to select the minimum element
        // Inner loop to find the minimum in the unsorted portion
    }
}